# External Secrets Operator — Secrets Manager → k8s Secrets
# See cloud_INFRASTRUCTURE.md § Secrets Management
installCRDs: true
serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: "" # Set to IRSA role ARN
