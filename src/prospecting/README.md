# Prospecting Service

ICP scoring + semantic search on extra fields. Ranks prospects by fit score.

Subscribes to: `prospecting.requested`  
Publishes: `prospecting.completed` (consumed **only** by the Orchestrator).

TODO: Implement service.
