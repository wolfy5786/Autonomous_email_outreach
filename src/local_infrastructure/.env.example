# Copy to .env and fill in. `.env` is gitignored.
# Only the key matching LLM_MODEL's provider prefix needs to be set.

LLM_MODEL=gemini/gemini-1.5-pro
GEMINI_API_KEY=
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
