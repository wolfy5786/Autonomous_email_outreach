/** Placeholder overview page — real content lands when campaigns/drafts pages are wired. */
export default function Overview() {
  return (
    <div>
      <h1 className="text-3xl font-semibold tracking-tight">Overview</h1>
      <p className="mt-2 text-black/60">
        Pipeline at a glance. Real metrics land once the campaigns and drafts
        pages are wired.
      </p>
    </div>
  );
}
