# Web UI

Static SPA — campaign management, prospects, draft status, pipeline monitoring.

Served by nginx. All API calls go to the Orchestrator via `/api/*`.

TODO: Implement SPA (React + Vite recommended).
